# Pleo Connector — UI Component Plan

## Sidebar UI (`panels.py`)
- Standardized vertical layout with `align="stretch"`.
- Clean header and description of available capabilities.
- Form controls for `label`, `api_key`, and optional `base_url`.
- Built-in `Modal` triggered by "How do I set this up?" with step-by-step guidance.
- Direct quick-link to `__panel__pleo_settings`.
